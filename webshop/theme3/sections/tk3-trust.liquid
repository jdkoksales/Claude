{{ 'tk3.css' | asset_url | stylesheet_tag }}

<div class="tk3">
  <div class="trust">
    <div class="wrap row">
      {%- for block in section.blocks -%}
        <span {{ block.shopify_attributes }}>{{ block.settings.text }}</span>
      {%- endfor -%}
    </div>
  </div>
</div>

{% schema %}
{
  "name": "TK3 vertrouwensbalk",
  "max_blocks": 8,
  "blocks": [
    {
      "type": "item",
      "name": "Punt",
      "settings": [{ "type": "text", "id": "text", "label": "Tekst", "default": "🛡️ Niet tevreden? 14 dagen retour, geld terug" }]
    }
  ],
  "presets": [
    {
      "name": "TK3 vertrouwensbalk",
      "blocks": [
        { "type": "item", "settings": { "text": "★ Uitstekend beoordeeld door ondernemers" } },
        { "type": "item", "settings": { "text": "🛡️ Niet tevreden? 14 dagen retour, geld terug" } },
        { "type": "item", "settings": { "text": "🚚 Vandaag besteld, vandaag verzonden" } },
        { "type": "item", "settings": { "text": "🔁 Later zelf een andere pagina erop" } },
        { "type": "item", "settings": { "text": "🇳🇱 Nederland" } }
      ]
    }
  ]
}
{% endschema %}
