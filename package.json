{
  "name": "claude-coach-app",
  "version": "4.0.0",
  "description": "Terugval-bestendige doelen-app met een deterministisch getriggerde AI-coach (Anthropic API): minimum/target-niveaus, dagen actief, comebacks, heatmaps, weekreview, export/import.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test tests/*.test.mjs",
    "check": "node scripts/check.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.69.0",
    "dotenv": "^16.4.7",
    "express": "^4.21.2"
  }
}
